# Batch File Renamer — Dependencies
#
# Required:
#   tkinter — included with standard Python installations (no pip install needed)
#
# Required for the modern GUI:
customtkinter>=5.2.0
#
# Optional — install to enable additional features:
#   pip install tkinterdnd2  # Drag-and-drop folder support (note: not compatible with customtkinter base class by default)
#
# Development / build:
#   pip install pytest        # Run tests
#   pip install pyinstaller   # Build standalone executable
